public enum Transition
{
    OnFloor,
    CutVinePlant,
    NotGrounded,
    FindLadder,
    VineTrapped,
    Grounded,
    ExitLadder
}

public enum DrawType
{
    Box,
    CutVine,
}

public enum GameState
{
    Running,
    Death
}